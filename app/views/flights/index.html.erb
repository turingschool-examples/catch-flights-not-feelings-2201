<p>Flight Numbers:</p>
<% @flights.each do |flight| %>
  <%= flight.number %>, 
    <% @airlines.each do |airline| %><br>
      <% if flight.airline_id == airline.id %>
        <%= airline.name %>
      <% end %>
    <% end %>
  <% flight.passengers.each do |passenger| %>
    <%= passenger.name %><br>
    <%= link_to "Remove #{passenger.name}", "/flights" %><br><br>
  <% end %><br><br>
<% end %>