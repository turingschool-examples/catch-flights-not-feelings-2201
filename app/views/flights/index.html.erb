<h1>Flights</h1>

<% @flights.each_with_index do |fligt, index| %>
<div id="flights-<%= index %>">
  <p>
  <%= flight.number %>
  <%= flight.airline_name %>
  </p>
  <p>Passengers: <% flight.passengers.each do |passenger| %>
    <%= passenger.name %>
  <% end %>
  </p>
</div>
<% end %>
